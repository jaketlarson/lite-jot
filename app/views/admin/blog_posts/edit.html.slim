article
  .row
    = simple_form_for @blog_post, :url => admin_blog_post_path(@blog_post) do |post|

      == post.input :title, :autofocus => true
      == post.input :body
      == post.input :public, :label => "Make blog post visible to public"

      button.right Publish
